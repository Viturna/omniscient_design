<style>
  .table-admin .col-links {
    white-space: normal;
    min-width: 250px;
    max-width: 350px;
  }
  .table-admin .col-links a {
    color: #007bff;
    word-break: break-all;
  }
  .table-admin .col-actions {
    white-space: nowrap;
  }
  .bool-true { color: #28a745; font-weight: 500; }
  .bool-false { color: #999; }

  /* Flexbox pour aligner recherche et bouton */
  .toolbar-admin {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
    margin: 20px 0;
  }
</style>
<section class="top-default-page">
  <%= link_to users_path do %>
    <svg width="36" height="36" viewBox="0 0 36 36" fill="none" xmlns="http://www.w3.org/2000/svg">
      <g clip-path="url(#clip0_3256_104)">
        <path fill-rule="evenodd" clip-rule="evenodd" d="M18 0C27.9345 0 36 8.0655 36 18C36 27.9345 27.9345 36 18 36C8.0655 36 0 27.9345 0 18C0 8.0655 8.0655 0 18 0ZM20.214 22.4325C21.696 21.825 22.8495 20.616 23.388 19.1085L27 9L16.992 12.5805C15.483 13.1205 14.2755 14.277 13.668 15.759L9.0705 27L20.214 22.4325ZM20.031 16.0035C20.8575 16.83 20.8575 18.1725 20.031 18.9975C19.2045 19.824 17.8635 19.824 17.037 18.9975C16.2105 18.1725 16.2105 16.83 17.037 16.0035C17.8635 15.177 19.2045 15.177 20.031 16.0035Z" fill="#FFF"/>
      </g>
      <defs>
        <clipPath id="clip0_3256_104">
          <rect width="36" height="36" fill="white"/>
        </clipPath>
      </defs>
    </svg>
  <% end %>
  <h1>Gestion des Établissements</h1>
</section>
<section class="grid-1">
  <div class="toolbar-admin">
    <%= form_with url: admin_etablissements_path, method: :get, local: true, class: "search-form", style: "display: flex; gap: 10px; flex: 1; max-width: 600px;" do |f| %>
      <%= f.text_field :query, 
            value: params[:query], 
            placeholder: "Rechercher par nom, UAI, ville...", 
            class: "form-control", style: "width: 100%;" %>
      <%= f.submit "Rechercher", class: "btn btn-primary" %>
      <% if params[:query].present? %>
        <%= link_to "X", admin_etablissements_path, class: "btn btn-secondary", title: "Effacer" %>
      <% end %>
    <% end %>
    <%= link_to admin_etablissements_path(format: :csv, query: params[:query]), class: "btn btn-green", style: "display: flex; align-items: center; gap: 8px;" do %>
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg>
      Exporter Actifs CSV
    <% end %>
  </div>
  <%= paginate @etablissements %>
  <div class="table-wrapper">
    <table>
      <thead>
        <tr>
          <th>Nom</th>
          <th style="min-width: 100px;">Utilisateurs</th>
 
          <th>UAI</th>
          <th>Statut</th>
          <th>Type</th>
          <th>Localisation</th>
          <th>Coordonnées</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @etablissements.each do |etab| %>
          <tr>
            <td>
              <strong><%= etab.name %></strong>
            </td>
            <td style="text-align: center;">
              <% count = etab.users_count %>
              <span class="badge <%= count > 0 ? 'badge-green' : 'badge-gray' %>" 
                    style="padding: 4px 10px; border-radius: 12px; background: <%= count > 0 ? '#d4edda' : '#eee' %>; color: <%= count > 0 ? '#155724' : '#666' %>; font-weight: 600;">
                <%= count %>
              </span>
            </td>
            <td><code><%= etab.uai %></code></td>
            <td><%= etab.statut_public_prive %></td>
            <td><%= etab.type_etablissement %></td>
            <td>
              <%= etab.city %> (<%= etab.academy %>)<br>
              <small style="color:#666;"><%= etab.region %></small>
            </td>
            <td class="col-links">
              <% if etab.phone.present? %><%= etab.phone %><br>
              <% end %>
              <% if etab.messagerie.present? %><%= mail_to etab.messagerie %><br>
              <% end %>
              <% if etab.website.present? %><%= link_to "Site web", etab.website, target: "_blank" %><% end %>
            </td>
            <td class="col-actions">
              <%= link_to "Modifier", edit_admin_etablissement_path(etab), class: "btn btn-sm btn-primary" %>
              <% if count == 0 %>
                <%= link_to "Supprimer", admin_etablissement_path(etab), 
                      class: "btn btn-sm btn-danger",
                      data: { turbo_method: :delete, turbo_confirm: "Supprimer cet établissement ?" } %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div style="margin-top: 20px;">
    <%= paginate @etablissements %>
  </div>
</section>